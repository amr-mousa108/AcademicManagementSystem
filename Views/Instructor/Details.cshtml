<!DOCTYPE html>
<html lang="en">
<head>
    <title>Instructor Details</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

    <div class="container my-5">
        <div class="card shadow-sm" style="max-width: 600px; margin:auto;">

            <!-- صورة المدرس مع Overlay الاسم -->
            <div style="
        height: 400px;
        position: relative;
        background-image: url('@Url.Content($"~/Images/{Model.ImgUrl}")');
        background-size: cover;
        background-position: top;
        border-top-left-radius: .25rem;
        border-top-right-radius: .25rem;">

                <h3 style="
            position: absolute;
            bottom: 10px;
            left: 20px;
            color: white;
            background-color: rgba(0,0,0,0.5);
            padding: 5px 10px;
            border-radius: 5px;">
                    @Model.Name
                </h3>
            </div>

            <!-- تفاصيل المدرس -->
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4 text-end">Salary:</dt>
                    <dd class="col-sm-8">@Model.Salary.ToString("C0")</dd>

                    <dt class="col-sm-4 text-end">Address:</dt>
                    <dd class="col-sm-8">@Model.Address</dd>

                    <dt class="col-sm-4 text-end">Department:</dt>
                    <dd class="col-sm-8">@Model.Department?.Name</dd>

                    <dt class="col-sm-4 text-end">Course:</dt>
                    <dd class="col-sm-8">@Model.Course?.Name</dd>
                </dl>
            </div>

            <!-- زر العودة -->
            <div class="card-footer text-center">
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        </div>
    </div>

</body>
</html>
